# =============================================================================
# Go Productions
# =============================================================================

$schema: "https://json-schema.org/draft/2020-12/schema"
$id: "https://exfs.org/schemas/languages/productions/go"
title: "Go Productions"
description: "Production capture rules for Go source files"

# -----------------------------------------------------------------------------
# Productions
# -----------------------------------------------------------------------------

productions:
  - comments    # // single or /* block */
  - imports     # import "fmt" or import (...)
  - packages    # package main
  - strings     # "hello" or `raw string`
  - variables   # var x int = 1
  - constants   # const x = 1
  - types       # type Foo = Bar (alias)
  - functions   # func foo() {}
  - methods     # func (t T) foo() {}
  - calls       # foo() or t.Method()
  - structs     # type Foo struct {}
  - interfaces  # type Foo interface {}

# -----------------------------------------------------------------------------
# Terminals
# -----------------------------------------------------------------------------

$defs:
  terminals:
    enum:
      # Base
      - exfs.go.comments
      - exfs.go.imports
      - exfs.go.packages
      - exfs.go.strings
      - exfs.go.variables
      - exfs.go.constants
      - exfs.go.types
      - exfs.go.functions
      - exfs.go.methods
      - exfs.go.calls
      - exfs.go.structs
      - exfs.go.interfaces

      # Modifiers
      - exfs.go.variablesWithConstants
      - exfs.go.functionsWithPrivate
      - exfs.go.functionsWithMethods
      - exfs.go.functionsWithGenerics
      - exfs.go.structsWithPrivate
      - exfs.go.structsWithGenerics
      - exfs.go.interfacesWithPrivate
